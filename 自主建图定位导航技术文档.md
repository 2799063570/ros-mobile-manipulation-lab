# 自主建图、定位与导航技术文档

> 本文档依据工程代码（
>
> `aubo_mobile_robot`
>
>  等包）的实际配置与实现整理，涉及的关键文件路径均已标注，可直接对照源码查阅。配套系统架构图见同目录 
>
> `自主建图定位导航_系统架构图.html`
>
> （浏览器打开）。

## 1. 系统概述

本系统基于 ROS 导航框架实现移动底盘的自主建图、定位与导航，并与机械臂抓取任务协同：



* **建图**采用 Gmapping 算法，**定位**采用 AMCL 算法，**导航**依托 move\_base 导航栈。Gmapping 与 AMCL 均属于基于粒子滤波的算法 —— 前者在未知环境探索中增量构建占据栅格地图，后者在已知地图中持续估计机器人位姿。

* **传感器输入**：前后双平面激光雷达（经融合节点合并为 `/scan`）、底盘里程计（`odom`）、RGB-D 相机（用于后续抓取感知）。

* **速度输出链路**：导航与任务层产生的速度指令统一先发布到 `/cmd_vel_raw`，经激光安全过滤节点校验后输出 `/cmd_vel` 下发底盘，保证任何来源的运动指令都不绕过急停保护。

* **任务协同**：导航只负责将底盘送到工位预停靠点；到达后任务层综合桌边间距、机械臂可达性与相机视野选择精停位姿。导航终点不仅是底盘可达点，同时为后续抓取作业提供有利位姿条件。

## 2. 算法选型与基本原理

### 2.1 建图：Gmapping（slam\_gmapping）

**原理简述**：Gmapping 是基于 RBPF（Rao-Blackwellized Particle Filter）的 2D SLAM 算法。每个粒子携带一条机器人历史轨迹与一张地图假设，通过激光观测更新粒子权重，并以融合里程计运动模型的提议分布采样，解决激光匹配与里程计漂移的联合估计问题。适用于中小规模室内环境的二维栅格建图。

**代码参数**（`aubo_mobile_navigation/config/gmapping.yaml`，节点 remap `scan→/scan`）：



| 参数                           | 值                 | 说明              |
| ---------------------------- | ----------------- | --------------- |
| particles                    | 40                | 粒子数             |
| map\_update\_interval        | 2.0 s             | 地图发布更新周期        |
| linearUpdate / angularUpdate | 0.10 m / 0.10 rad | 位移 / 旋转阈值触发扫描匹配 |
| temporalUpdate               | 1.0 s             | 时间阈值触发匹配        |
| maxUrange / maxRange         | 18.0 / 20.0 m     | 激光有效 / 最大量程     |
| delta                        | 0.05 m            | 栅格分辨率（5 cm）     |
| xmin/xmax/ymin/ymax          | ±20.0 m           | 地图边界范围          |
| minimumScore                 | 50                | 扫描匹配最低得分        |

### 2.2 定位：AMCL（amcl）

**原理简述**：AMCL（Adaptive Monte Carlo Localization）以粒子滤波估计机器人在已知地图中的位姿。粒子数通过 KLD（Kullback-Leibler Divergence）采样自适应增减；运动模型为差分底盘模型（`odom_model_type: diff`），观测模型为似然域模型（`likelihood_field`）。

**代码参数**（`aubo_mobile_navigation/config/amcl.yaml`）：



| 参数                                                    | 值                                | 说明           |
| ----------------------------------------------------- | -------------------------------- | ------------ |
| min\_particles / max\_particles                       | 500 / 2000                       | 自适应粒子数上下限    |
| kld\_err / kld\_z                                     | 0.02 / 0.99                      | KLD 采样误差与置信度 |
| update\_min\_d / update\_min\_a                       | 0.08 m / 0.08 rad                | 位姿变化触发更新阈值   |
| laser\_max\_beams                                     | 90                               | 每帧使用的激光束数    |
| laser\_min\_range / laser\_max\_range                 | 0.12 / 18.0 m                    | 激光有效范围       |
| laser\_model\_type                                    | likelihood\_field                | 似然域观测模型      |
| laser\_likelihood\_max\_dist                          | 2.0                              | 似然域最大匹配距离    |
| odom\_model\_type                                     | diff                             | 差分底盘运动模型     |
| odom\_alpha1..5                                       | 0.15 / 0.15 / 0.12 / 0.12 / 0.10 | 里程计噪声参数      |
| global\_frame\_id / odom\_frame\_id / base\_frame\_id | map / odom / base\_footprint     | 坐标框架         |
| transform\_tolerance                                  | 0.5 s                            | TF 容忍时间      |

### 2.3 导航：move\_base

move\_base 是导航总控节点：通过 action 接口接收目标位姿，以全局 / 局部代价地图规划并控制机器人运动，内置恢复行为（recovery）。

**代码参数**（`aubo_mobile_navigation/config/move_base.yaml`）：



| 参数                                           | 值                                 | 说明                                             |
| -------------------------------------------- | --------------------------------- | ---------------------------------------------- |
| base\_global\_planner                        | navfn/NavfnROS                    | 全局规划器插件（**注意**：与正文 “GlobalPlanner” 不一致，见 §4.4） |
| base\_local\_planner                         | dwa\_local\_planner/DWAPlannerROS | 局部规划器插件                                        |
| controller\_frequency                        | 10.0 Hz                           | 局部控制频率                                         |
| planner\_frequency                           | 1.0 Hz                            | 全局重规划频率                                        |
| controller\_patience / planner\_patience     | 5.0 / 5.0 s                       | 控制 / 规划失败容忍时间                                  |
| conservative\_reset\_dist                    | 3.0 m                             | 保守重置距离                                         |
| recovery\_behavior\_enabled                  | true                              | 恢复行为开关                                         |
| clearing\_rotation\_allowed                  | true                              | 允许旋转清除（近场场景由启动文件禁用）                            |
| oscillation\_timeout / oscillation\_distance | 10.0 s / 0.20 m                   | 振荡检测                                           |

## 3. 建图与定位的衔接

### 3.1 探索建图阶段

启动文件：`aubo_mobile_navigation/launch/mapping.launch`（配合 `map_saver.launch` 保存地图）。



1. **双激光合并**：`scan_merger.launch` 启动 `dual_laser_merger.py`，订阅 `/front/scan` 与 `/rear/scan`，将两帧激光点先平移、旋转到 `base_footprint` 系，再按方位角合并、同一角度栅格保留最近距离，输出 `/scan`（15 Hz、720 采样点、量程 0.12–20 m、要求前后雷达数据新鲜，`max_scan_age` 0.5 s）。

2. **构建栅格地图**：`slam_gmapping` 以 `/scan` 与 `/odom` 为输入，输出 `/map`（nav\_msgs/OccupancyGrid）。

3. **地图语义**：栅格值 **-1 表示未知区域，0 附近表示自由空间，较高值（趋近 100）表示障碍物**。

4. **保存地图**：`map_saver` 将 `/map` 保存为 `.pgm` 图像 + `.yaml` 元数据。工程自带地图示例（`aubo_mobile_nav_sorting/maps/`）：

* `nav_sorting.yaml`：分辨率 0.20 m、原点 `[-5.0, -3.0, 0.0]`；

* `four_tables.yaml`：分辨率 0.20 m、原点 `[-6.0, -4.0, 0.0]`；

* 两者 `occupied_thresh` 0.65、`free_thresh` 0.196。

### 3.2 复用地图导航阶段

启动文件：`aubo_mobile_navigation/launch/navigation.launch`。



1. **map\_server** 加载地图文件（arg `map_file`），以静态地图层发布 `/map`。

2. **AMCL 定位**：订阅 `/scan`（remap）与里程计，估计机器人在 **map 系**下的位姿，发布 `map→odom` TF 与 `/amcl_pose`、粒子可视化。Gazebo 场景中通过 `publish_initial_pose.py` 自动注入初始位姿（如 `mission_gazebo.launch` 中 x=-2.50、y=0、yaw=0）；实车可在 RViz 中 2D Pose Estimate 手动给定。

3. **move\_base 规划导航**：以全局代价地图（静态层 + 障碍层 + 膨胀层）做全局路径规划，以局部代价地图（滚动窗口）实时避障并输出速度。

4. **坐标框架（TF）链路**：`map → odom → base_footprint → base_link → 各传感器（雷达/相机）`。

> 注：
>
> `navigation.launch`
>
>  中 
>
> `nav_goal_topic`
>
> （默认 
>
> `/move_base_simple/goal`
>
> ）可被改为自定义话题，让任务节点先接收 RViz 的 2D Nav Goal 数据、决定是否执行导航，再转发给 move_base。

## 4. 路径规划与代价地图细节

### 4.1 全局代价地图（`config/global_costmap.yaml`）



* `global_frame: map`，`robot_base_frame: base_footprint`；更新 5 Hz、发布 2 Hz；

* `static_map: true`（基于 map\_server 静态地图），`track_unknown_space: true`；

* 插件：`static_layer`（静态地图）+ `obstacle_layer`（实时障碍）+ `inflation_layer`（膨胀）。

### 4.2 局部代价地图（`config/local_costmap.yaml`）



* `global_frame: odom`；`rolling_window: true`，6 m × 6 m，分辨率 0.05 m；

* 更新 10 Hz、发布 5 Hz；

* 插件：`obstacle_layer` + `inflation_layer`。

### 4.3 公共代价地图参数（`config/costmap_common.yaml`）



| 参数                                | 值                                    | 说明                                                  |
| --------------------------------- | ------------------------------------ | --------------------------------------------------- |
| robot\_radius                     | 0.35 m                               | 底盘圆形包络（仿真底盘半径 0.34 m + 余量，`footprint_padding` 0.01） |
| obstacle\_range / raytrace\_range | 6.0 / 8.0 m                          | 障碍标记 / 射线清除范围                                       |
| max\_obstacle\_height             | 2.0 m                                | 障碍物高度上限                                             |
| 观测源 merged\_laser                 | topic `/scan`、frame `base_footprint` | 合并后的激光                                              |
| inflation\_radius                 | 0.50 m                               | 膨胀半径                                                |
| cost\_scaling\_factor             | 3.5                                  | 代价衰减系数                                              |

### 4.4 全局规划器：正文口径与代码差异（**需确认**）



* 平台通用配置（`turn_on_wheeltec_robot/params_nav_common/move_base_params.yaml`、`wheeltec_robot_gazebo/param_common/move_base_params.yaml`）默认使用 `global_planner/GlobalPlanner`；

* 但移动操作机器人实际导航包 `aubo_mobile_navigation/config/move_base.yaml` 中明确配置为 `navfn/NavfnROS`，启动时以该文件为准；

* 四桌场景的 `four_tables_navigation.yaml` 未覆盖 `base_global_planner`，因此同样继承 navfn。

**结论**：正文 “这里我们使用 GlobalPlanner” 与实际生效配置不一致，需二选一对齐：① 将 `aubo_mobile_navigation/config/move_base.yaml` 改为 `global_planner/GlobalPlanner`；② 文档中改为 NavfnROS（navfn 为基于 Dijkstra 波前扩展的传统全局规划器）。

### 4.5 局部规划器

**默认 DWA**（`config/dwa_local_planner.yaml`）关键参数：



| 参数                                         | 值                     | 说明     |
| ------------------------------------------ | --------------------- | ------ |
| max\_vel\_x / max\_vel\_theta              | 0.30 m/s / 0.80 rad/s | 速度上限   |
| acc\_lim\_x / acc\_lim\_theta              | 0.50 / 1.00           | 加速度上限  |
| vx\_samples / vtheta\_samples              | 15 / 30               | 速度采样密度 |
| sim\_time                                  | 2.0 s                 | 轨迹预测时间 |
| xy\_goal\_tolerance / yaw\_goal\_tolerance | 0.04 m / 0.06 rad     | 目标容差   |
| path/goal/occ 权重                           | 32 / 20 / 0.05        | 轨迹评分权重 |
| holonomic\_robot                           | false                 | 差速底盘   |

**四桌场景专用导航 profile**（`config/four_tables_navigation.yaml`，由 `four_tables_gazebo.launch` 以 `use_navigation_profile=true` 加载）：



* `planner_frequency: 0.0`：取消周期性全局重规划，仅在目标更新 / 路径失效时规划；`controller_frequency` 12 Hz；

* DWA 与 TEB 双配置并存，启动时用 arg `local_planner` 选择（默认 `teb`：`teb_local_planner/TebLocalPlannerROS`）；

* 预停点位置容差收紧到 `xy_goal_tolerance: 0.01 m`、航向 0.08 rad（`latch_xy_goal_tolerance: true`），剩余航向误差由任务节点闭环对正；

* 允许小幅倒车修正：DWA `min_vel_x: -0.06`，`max_vel_theta: 0.35`，避免桌椅附近转向振荡；

* TEB 显式指定圆形优化轮廓 `footprint_model.radius: 0.35`，避免点模型规划出贴桌轨迹；

* 近场禁用旋转恢复（`clearing_rotation_allowed=false`）。

## 5. 速度指令与激光安全过滤链路

### 5.1 链路结构

`navigation.launch` 中 `laser_safety=true`（默认）时：



```
move\_base（规划输出）→ /cmd\_vel\_raw → laser\_safety\_filter（/scan 校验）→ /cmd\_vel → 底盘控制器
```

`laser_safety=false` 时 move\_base 直连 `/cmd_vel`（仅用于调试）。

### 5.2 激光安全过滤节点（`scripts/laser_safety_filter.py`）

节点为纯 Python 实现的 “速度输出前停车层”，20 Hz 定时发布，组合以下检查（任一不满足即输出零速）：



1. **运动互锁**：订阅 `/sorting/base_locked`（Bool）。机械臂作业期间置 True，底盘强制零速。

2. **数据超时**：指令（`command_timeout` 0.5 s）或激光（`scan_timeout` 0.5 s）无更新 → 零速。

3. **障碍距离检查**（核心）：

* **动态停车距离**：`d_stop = 基础余量(stop_distance 0.35 m / reverse_stop_distance 0.35 m) + 反应时间位移(reaction_time 0.25 s × v) + 匀减速制动距离(v² / (2 × max_deceleration 0.8 m/s²))`，前进 / 后退分别计算；

* **障碍走廊**：以机器人半宽 + 安全余量（`robot_half_width` 0.30 + `safety_margin` 0.12 = 0.42 m）为走廊半宽，检查前进方向 `(0, d_forward]`、后退方向 `[-d_reverse, 0)` 内的激光点；

* **旋转安全**：纯旋转时要求 `rotation_clearance` 0.48 m 内无点；转向耦合（低速平移 + 旋转）时按扫描扇形包络矩形检查；

* **噪点抑制**：同一连续角度区间内障碍点簇 ≥ `min_cluster_points`（3）才判阻塞；但落在 `emergency_distance`（0.18 m）内的单点立即触发急停；

* **状态输出**：`/laser_safety_filter/blocked`（latch），供上层监控。

**关键参数**（`laser_safety_filter.launch` 默认值）：



| 参数                                       | 值                     | 说明          |
| ---------------------------------------- | --------------------- | ----------- |
| robot\_half\_width                       | 0.30 m                | 机器人半宽       |
| safety\_margin                           | 0.12 m                | 安全余量        |
| stop\_distance / reverse\_stop\_distance | 0.35 / 0.35 m         | 前 / 后基础停车距离 |
| rotation\_clearance                      | 0.48 m                | 旋转净空        |
| reaction\_time                           | 0.25 s                | 反应时间        |
| max\_deceleration                        | 0.8 m/s²              | 最大减速度       |
| min\_cluster\_points                     | 3                     | 连续障碍点簇阈值    |
| emergency\_distance                      | 0.18 m                | 急停距离        |
| scan\_timeout / command\_timeout         | 0.5 / 0.5 s           | 数据超时        |
| motion\_lock\_topic                      | /sorting/base\_locked | 运动互锁话题      |

### 5.3 任务层近场移动同样经过安全链路

任务节点所有低速底盘运动（航向对正、直线精靠、分拣后离桌、恢复性小幅移动）都发布到 `/cmd_vel_raw`（`base_recovery_cmd_vel_topic` 默认值），由 `laser_safety_filter` 统一过滤后再下发 `/cmd_vel`，即 “近场直行停靠也走这条安全过滤链路”，不绕过急停保护。

## 6. 任务层：预停靠与精停位姿决策

任务节点：`aubo_mobile_nav_sorting/scripts/nav_sorting_mission.py`（Python）与 `src/navigation_sorting_mission.cpp`（C++，`mission.launch` 默认 cpp），两实现参数、话题、服务完全一致。

### 6.1 两级停靠流程（以单工位 `scenario.yaml` 为例）



1. 任务开始前机械臂收至 transport 位（`/sorting/home`）；

2. **move\_base 导航到预停靠点** `pre_dock_goal`（如 `[1.85, 0.0, 0.0]`）—— 导航只负责到预停靠点，预停靠精度由 move\_base 保证（四桌场景收紧到 1 cm）；

3. **近场候选位姿生成与评分**（见 §6.2）：若可行，先做一次小角度航向对正（最大 0.12 rad ≈ 6.9°，速度 0.12 rad/s，目标误差 0.015 rad），再以 0.04 m/s 低速**直线精靠**到选中位姿；

4. 精停后：机械臂到工作姿态（`/sorting/prepare_work`）→ 相机观察与 MoveIt 规划验证（`/sorting/move_to_observation`）→ 分拣（`/sorting/start`）；

5. 观察 / 分拣失败：收臂回退 pre\_dock（直行精靠失败时），尝试下一候选；候选耗尽则任务失败；

6. 每工位完成后收臂并后退离桌（`post_sort_retreat_distance` 0.30 m，四桌场景）；全部完成后导航返回出发点（`return_to_start`）。

### 6.2 近场候选位姿生成与评分（`_score_candidate`）



* **候选集合**（`scenario.yaml`）：x ∈ {2.16, 2.10, 2.06}，y ∈ {0.0, -0.08, 0.08}，yaw ∈ {0.0, -0.08, 0.08}，最多 6 个（`near_field_max_candidates`）；

* **硬约束**（任一不满足即剔除该候选）：


  * **桌边间距**：底盘中心到桌体包围盒（`near_field_table [3.00, 0.0, 0.80, 1.20]`）的最短距离 ≥ `near_field_base_clearance`（0.40 m）；

  * **检测 / 可达工作区**：所有工件点（`near_field_workpieces`，如 \[2.78,-0.12]、\[2.86,0]、\[2.78,0.12]）在 base 系下的投影必须落在检测器工作区 `[0.40, 0.82, -0.22, 0.22]` 内；

* **评分**（越小越优）：

  `score = camera_error + 0.15 × nominal_error + 0.05 × yaw_error − 0.02 × clearance`


  * `camera_error`：各工件点相对相机中心射线落点（`near_field_camera_target [0.626, -0.077]`）的平方距离之和 —— 对应相机视野约束；

  * `nominal_error`：候选与最终目标位姿（`sorting_goal`）的平面距离；`yaw_error` 为偏航误差；`clearance` 为桌边距（越大越优，作奖励项）。

* **直行精靠可行性**（`_can_direct_dock_from_pre_dock`）：候选相对 pre\_dock 必须满足纵向位移 ∈ (0, 0.50 m]、横向 ≤ 0.04–0.08 m、偏航 ≤ 0.04–0.06 rad（`direct_dock_*` 系列参数），否则不使用直行控制器，退回 move\_base 导航到该候选；

* 每次精停后由 MoveIt 与真实检测共同验证；失败则收臂并尝试下一候选（候选间回退到 pre\_dock，避免在桌旁转圈）。

### 6.3 闭环精靠控制器（`_drive_straight_to`）



* **位姿来源**：TF（map→base\_footprint，pose 年龄 ≤ `base_pose_max_age` 0.5 s，过期则终止本次任务的底盘移动）；

* **控制律**：在目标坐标系下解算纵向误差 forward、横向 cross 与偏航误差；线速度 = 0.04 m/s（`base_recovery_speed`），角速度 = `2 × (yaw_error + steering)` 限幅于 `heading_speed`（0.12 rad/s），steering 为横向误差的比例校正（限幅 0.04 rad）；

* **收敛判定**：平面位置误差 ≤ `direct_dock_goal_tolerance`（0.06–0.07 m）且航向 ≤ `heading_final_tolerance`（0.025–0.04 rad）；

* **保护机制**：直行超时 15 s、无进展 2.5 s（`progress_epsilon` 0.005 m）即安全退出；若被激光安全层持续拦停，安全退出且**不再交回 DWA 旋转恢复**—— 体现 “精停服务于抓取且以安全为边界”；

* 全部运动指令发布到 `/cmd_vel_raw`，经 §5 安全链路下发。

### 6.4 容错与重试



* **导航重试**：每次工位导航可重试 `navigation_retries`（1）次，失败先调用 `/move_base/clear_costmaps` 清除代价地图再重试；

* **任务状态机**：`/nav_sorting/state` 发布（IDLE / NAVIGATING / COORDINATING / DIRECT\_DOCKING / SORTING / SUCCEEDED / FAILED 等）；`/nav_sorting/start`、`/nav_sorting/stop` 服务控制任务启停；

* **动态参数**：任务运行中参数锁定（dynamic\_reconfigure 拒绝任务中修改），保证整段任务参数一致。

## 7. 关键文件索引



| 文件                                                                         | 作用                                           |
| -------------------------------------------------------------------------- | -------------------------------------------- |
| aubo\_mobile\_navigation/launch/mapping.launch                             | 建图启动（scan\_merger + slam\_gmapping + rviz）   |
| aubo\_mobile\_navigation/launch/navigation.launch                          | 导航启动（map\_server + amcl + move\_base + 安全过滤） |
| aubo\_mobile\_navigation/launch/scan\_merger.launch                        | 双激光合并节点启动                                    |
| aubo\_mobile\_navigation/launch/laser\_safety\_filter.launch               | 激光安全过滤参数                                     |
| aubo\_mobile\_navigation/launch/move\_base.launch.xml                      | move\_base 参数装载                              |
| aubo\_mobile\_navigation/config/gmapping.yaml                              | Gmapping 参数                                  |
| aubo\_mobile\_navigation/config/amcl.yaml                                  | AMCL 参数                                      |
| aubo\_mobile\_navigation/config/move\_base.yaml                            | move\_base / 规划器参数                           |
| aubo\_mobile\_navigation/config/costmap\_common.yaml                       | 代价地图公共参数                                     |
| aubo\_mobile\_navigation/config/global\_costmap.yaml / local\_costmap.yaml | 全局 / 局部代价地图                                  |
| aubo\_mobile\_navigation/config/dwa\_local\_planner.yaml                   | DWA 局部规划器参数                                  |
| aubo\_mobile\_navigation/scripts/dual\_laser\_merger.py                    | 双激光融合实现                                      |
| aubo\_mobile\_navigation/scripts/laser\_safety\_filter.py                  | 激光安全过滤实现                                     |
| aubo\_mobile\_nav\_sorting/launch/mission.launch                           | 任务节点启动（python/cpp 可选）                        |
| aubo\_mobile\_nav\_sorting/launch/navigation\_sorting.launch               | 实机导航 + 分拣整链路启动                               |
| aubo\_mobile\_nav\_sorting/launch/mission\_gazebo.launch                   | 单工位仿真整链路启动                                   |
| aubo\_mobile\_nav\_sorting/launch/four\_tables\_gazebo.launch              | 四工位仿真整链路启动                                   |
| aubo\_mobile\_nav\_sorting/config/scenario.yaml                            | 单工位任务参数（预停靠、候选、精靠）                           |
| aubo\_mobile\_nav\_sorting/config/four\_tables.yaml                        | 四工位任务参数（工位 / 预停靠 / 放置点）                      |
| aubo\_mobile\_nav\_sorting/config/four\_tables\_navigation.yaml            | 四桌场景导航 profile（DWA/TEB）                      |
| aubo\_mobile\_nav\_sorting/scripts/nav\_sorting\_mission.py                | 任务节点 Python 实现                               |
| aubo\_mobile\_nav\_sorting/src/navigation\_sorting\_mission.cpp            | 任务节点 C++ 实现                                  |
| aubo\_mobile\_nav\_sorting/maps/nav\_sorting.yaml                          | 单工位地图元数据                                     |
| aubo\_mobile\_nav\_sorting/maps/four\_tables.yaml                          | 四桌地图元数据                                      |

## 8. 待确认 / 差异清单



1. **全局规划器口径**：正文称使用 GlobalPlanner，而 `aubo_mobile_navigation/config/move_base.yaml` 实际为 `navfn/NavfnROS`（平台默认配置为 GlobalPlanner）。需统一：改配置或改文档。

2. **地图分辨率**：Gmapping `delta` 为 0.05 m，但工程自带地图元数据为 0.20 m—— 地图保存时可能经过降采样或使用了不同建图配置；如需要更高定位精度，建议按 0.05 m 重新建图 / 保存。

3. **里程计坐标系**：`amcl.yaml` 使用 `odom_frame_id: odom`；平台 bringup 中 `robot_pose_ekf` 默认输出 `odom_combined`。需按实际整机联调确认使用的里程计话题与坐标系（仿真中 Gazebo 直接提供 odom）。