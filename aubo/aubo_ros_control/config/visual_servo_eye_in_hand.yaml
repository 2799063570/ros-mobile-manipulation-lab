servo_mode: eye_in_hand
camera_link: camera_color_optical_frame
control_link: tcp_link

# 期望目标位姿在夹爪 TCP 坐标系中定义，X/Y 为零表示目标与夹爪中心线对齐，
# 而不是与相机光轴对齐。相机光心相对夹爪沿 X 偏移 60 mm，沿接近方向比
# TCP 后缩约 35 mm；因此 0.115 m 的 TCP 距离对应约 0.15 m 的相机深度。
# 控制器通过 TF 自动考虑该 X 偏移，不需要在 desired_target_position 中重复补偿。
desired_target_position: [0.0, 0.0, 0.115]
# Independent controller-side guard. This remains below the desired distance
# and matches the perception frontend's minimum valid depth.
minimum_safe_target_distance: 0.10
desired_target_rpy: [0.0, 0.0, 0.0]
target_offset: [0.0, 0.0, 0.0]
use_orientation_control: false

# Eye-in-hand measurements arrive more slowly than the 100 Hz control loop.
# Conservative Cartesian gain/speed plus stronger DLS damping suppress delayed
# corrections and joint amplification near singular configurations.
linear_gain: 0.30
max_linear_velocity: 0.025
angular_gain: 0.30
max_angular_velocity: 0.12
position_deadband: 0.006
alignment_hold_time: 0.35
alignment_release_multiplier: 2.0
dls_lambda: 0.08
tracking_velocity_filter_alpha: 0.15
target_timeout: 0.40

# The shared sorting scene puts all targets on the table. Use the same joint
# posture as the sorting "observe" SRDF target so the wrist camera faces that
# workspace before first detection and after target loss.
initial_search_posture: [0.08648, -0.54623, 0.06780, -0.91058, 1.58105, 1.82491]
recovery_posture: [0.08648, -0.54623, 0.06780, -0.91058, 1.58105, 1.82491]
initial_search_enabled: true
# Brief dropouts hold the current pose. Only a sustained loss returns toward
# the observation posture, and tracking resumes after a stable reacquisition.
loss_strategy: coast_then_open
coast_duration: 0.0
recovery_delay: 1.5
reacquire_hold_time: 0.40
