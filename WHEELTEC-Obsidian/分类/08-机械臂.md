# 机械臂

## Aubo 包组

- `aubo_description`：机械臂 URDF 和显示启动文件。
- `aubo_gazebo`：Gazebo 世界、控制器和 bringup。
- `aubo_moveit_config`：MoveIt 规划、执行、RViz 和控制器配置。

## 建议学习前置

1. URDF、joint 与 TF。
2. `ros_control` 控制器。
3. MoveIt planning group 与末端执行器。
4. 运动规划和碰撞场景。

机械臂是相对独立的子系统，建议在移动底盘主线学完后再进入。

