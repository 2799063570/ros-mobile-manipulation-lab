# 基础库与消息包

这类包通常不直接提供用户功能，但会被驱动或算法包依赖。

- `lslidar_msgs`、`lslidar_x10_msgs`：雷达消息定义。
- `nav_core`：全局规划器、局部规划器和恢复行为的插件接口。
- `voxel_grid`：体素数据结构。
- `sparse_bundle_adjustment`：Karto 使用的优化组件。
- `ddynamic_reconfigure`：运行时动态参数支持。
- `world_canvas_msgs`：地图保存相关组件。

学习这些包时先查看“谁依赖它”，再决定是否需要深入源码。

