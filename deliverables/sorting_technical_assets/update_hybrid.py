from pathlib import Path
p=Path(__file__).with_name('build_document.py')
s=p.read_text(encoding='utf-8')
changes={
'两种真机输出链路互斥':'独立RobotHW与伺服SDK输出互斥',
'分拣节点和视觉伺服节点均已有独立实现。现有抓放状态机直接调用MoveIt与夹爪动作，混合流程中的自动控制权交接需由任务编排显式接入。视觉伺服真机后端直接调用SDK，并不经过RobotHW的轨迹队列。':'常规分拣通过MoveIt Action与RobotHW执行；混合控制则在同一个VisualServo节点中请求MoveIt规划服务，将远距离轨迹和近距离PBVS共同送入伺服队列与同一输出后端，避免两套SDK同时控制。完整夹取、搬运和放置仍由分拣任务层组织。[S9]',
'VisualServo  100 Hz\\n位姿误差 → 阻尼伪逆 → 位置积分':'VisualServo  100 Hz\\n混合模式  MoveIt规划服务＋近距PBVS',
'伺服队列容量为80点，满时淘汰最旧待执行点':'标准伺服队列容量为80点，混合模式配置缩短为8点；满时淘汰最旧待执行点',
'CommandQueue  容量 80 点':'CommandQueue  标准80点  混合8点',
'混合控制的目标是用MoveIt完成较大范围的避障运动，在预抓取区域用视觉闭环修正剩余位置误差，随后完成接近、夹取与搬运。下图表达系统级编排设计；控制权交接属于需要显式实现和验证的接入环节。':'混合控制在同一个VisualServo节点中完成远距离规划与近距离PBVS切换。通过/plan_kinematic_path只请求规划结果，校验关节顺序、时间、限位及起终点后，以统一队列输出轨迹采样点；该路径不使用MoveIt轨迹Action执行。[S9]',
'初始化  标定与场景检查  观测并稳定目标':'初始化与反馈保持  等待新鲜目标',
'MoveIt 长距离规划 → 到达预抓取位置':'距离较远 → PLANNING  10 Hz检查待规划请求',
'控制权交接  设计接入点\\n结束轨迹  退出原控制模式  用反馈初始化伺服':'MoveIt服务规划 → 校验轨迹 → APPROACH\\n100 Hz路径采样  共用队列与SDK输出',
'视觉伺服 100 Hz → 误差收敛并持续保持':'达到接近点并稳定 → 近距PBVS 100 Hz\\n进入阈值0.10 m  退出阈值0.16 m',
'停止伺服并完成控制权交还\\n接近与闭合 → 抬升 → 搬运 → 放置':'TRACKING → ALIGNED  对准位保持\\n目标漂移或超时 → 停止旧路径并重新评估',
'退离与结果记录 → 下一目标':'后续任务衔接  接近与夹取  搬运  放置\\n完整抓放仍由任务层组织',
'图5  长距离规划与近距离伺服的混合流程设计':'图5  同一输出后端下的混合控制流程与后续抓放接口',
"h('交接条件')":"h('切换条件与规划并发')",
'进入伺服前，应确认上一条轨迹结束，原输出链路停止并退出独占控制模式，再以实时关节反馈初始化伺服积分器和队列。伺服达到稳定对齐后，应停止伺服、完成后端退出和控制权交还，再发起后续抓放动作。仅停止ROS轨迹控制器并不能证明SDK控制权已释放。':'hybrid_enabled默认关闭。启用后，TCP到期望TCP的距离≤0.10 m进入PBVS，>0.16 m退出近距状态，迟滞避免边界反复切换。远距路径规划到距期望位0.06 m的接近点，预算3 s，执行超时30 s。规划回调按10 Hz检查待处理请求，服务调用期间不持有控制锁，其余回调仍能处理停止和反馈。',
'当前默认位置死区为4 mm':'混合路径完成后先稳定0.3 s，再进入近距控制；目标位移超过0.04 m会使旧路径失效。当前默认位置死区为4 mm',
'眼在手外默认采用stop策略。上述阈值':'眼在手外默认采用stop策略；混合模式目标丢失时清除旧路径并保持。上述阈值',
"('规划与伺服争用控制柜','互斥后端与明确的进入退出顺序','混合编排应验证中止和交接失败路径')":"('规划与伺服争用控制柜','混合模式共用队列和唯一输出后端','外部RobotHW不能同时独占SDK')",
'混合流程的自动切换，应与已验证的控制权交接记录一并展示。':'混合模式可继续展示远距APPROACH到近距TRACKING的状态切换，结合运行日志说明阈值与目标丢失行为。',
'系统进一步设计了长距离规划与近距离视觉伺服的混合流程。':'混合模式通过MoveIt规划服务与同一执行队列，衔接长距离规划和近距离视觉闭环。',
"('伺服ROS回调及监测','AsyncSpinner(3)＋主线程','3个回调线程；监测10 Hz','监测不是控制计算循环'),":"('混合规划检查','ROS定时回调','10 Hz / 100 ms','有请求才调用MoveIt服务'),\n+ ('伺服ROS回调及监测','AsyncSpinner(3)＋主线程','3个回调线程；监测10 Hz','监测不是控制计算循环'),".replace('\n+','\n'),
"('S8','realsense":"('S9','aubo/aubo_ros_control/src/hybrid_control.cpp\\naubo/aubo_ros_control/config/hybrid_control.yaml','规划服务、轨迹校验、距离迟滞、短队列与状态切换'),\n+ ('S8','realsense".replace('\n+','\n'),
}
for a,b in changes.items():
    if a not in s:raise ValueError(a)
    s=s.replace(a,b)
p.write_text(s,encoding='utf-8')
